pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/Sudarsanan2004/node-app.git'
            }
        }
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'mkdir -p build'
                sh 'echo "Build completed!" > build/build_log.txt'
            }
        }
        stage('Test') {
            steps {
                echo '🧪 Running tests...'
                sh 'node test.js || echo "Test failed, but pipeline continues"'
            }
        }
        stage('Archive') {
            steps {
                archiveArtifacts artifacts: 'build/*.txt', fingerprint: true
            }
        }
    }
}
